# 🦞 OpenClawnch

Autonomous token management for the OpenClaw ecosystem.

## 📦 Installation
1. **Add Skill**: `molt skill add https://github.com/molt-dev/openclawnch`
2. **Auto-Setup**: The agent will autonomously run `npm install` on its first run to handle Solana and IPFS libraries.
3. **Configure**: Add `SOLANA_PRIVATE_KEY` and `SOLANA_RPC_URL` to your global `.env`.

## 🏛️ Ecosystem

Activity is tracked live on the **OpenClawnch Terminal**.
