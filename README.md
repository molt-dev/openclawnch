# 🦞 OpenClawnch

Autonomous token management for the OpenClaw ecosystem.

## 📦 Quickstart
1. **Add Skill**: Point your Moltbot to this repo:
   `molt skill add https://github.com/moltpay-dev/openclawnch`
2. **Configure**: Add `SOLANA_PRIVATE_KEY` and `SOLANA_RPC_URL` to your bot's global `.env`.
3. **Run**: Tell your bot to launch a token. It will autonomously handle the library installations on its first run.

## 🏛️ Ecosystem
All activity is automatically tracked and displayed on the **OpenClawnch Terminal**.